# Redesign — 2026-08-31 pass

**Status: plan approved-to-write by Ian's 2026-08-31 request ("keep what works, repair what's salvageable, gut and rebuild the rest"). GO GIVEN 2026-09-02 — Ian: "begin coding from the dev plan." Execution follows DEV_PLAN.md.**

Resolves the open 2026-06-12 decision (options A–D in [research/2026-06-10_review_findings.md](research/2026-06-10_review_findings.md)) with **option E: retire the momentum signal entirely and rebuild MICRO as the premarket briefing layer** over the two systems that carry real state — the MACRO dashboard and the trading bot.

## Why E, not A–D

Out-of-sample data since the June review killed the flip options. Scored 2026-08-31:

| Slice (5d market-neutral pairs) | n | Win | Mean/trade | Sharpe/trade |
|---|---|---|---|---|
| Full live record (5/6–8/31) | 210 | 43% | −0.82% | −0.20 |
| Pre-review (≤ 6/12) | 69 | 32% | −2.29% | −0.52 |
| Post-review (> 6/12) | 141 | 48% | −0.11% | −0.03 |
| Post-review, top pair only | 39 | 54% | **+0.58%** | +0.14 |

Momentum was strongly negative before the review and mildly *positive* after — a June flip to reversion would have lost money since. Combined with the 2-year backtest (sign-unstable) and the bot's 33-year result (Sharpe 0.19), the conclusion is: **no stable edge in either direction at any horizon tested.**

*Same-session correction (fable_plan.md Part IV): the table's directional slices are overlap-inflated — non-overlapping, the live record is n=15 full-period with a sign that flips with entry convention, and post-review is n=9, t≈1. The honest statement is stronger for option E: the live data never had power to establish any direction; the only powered estimate is the 33-year Sharpe 0.19 ≈ no edge.* Confidence field confirmed anti-predictive over the live record (corr spread↔fwd return −0.14; high-spread half −1.09%/trade vs −0.56% low half). The 1.5% spread gate never once withheld publication in 75 run-days.

## KEEP — proven, untouched

- Task Scheduler stack: main 5:30 CT + wake 5:20 CT + watchdog 7:00 CT, `-WakeToRun`, battery flags.
- NYSE holiday calendar + ET date handling in `run_tactical.py`.
- `src/pushover.py`, the 6:30 ET delivery slot, single-push format.
- JSONL append-log discipline (every run logged, including error days).
- Two-repo git flow, CLAUDE.md conventions, rule-based-heuristic constraint. The ≥30-labeled-trades ML gate stands — note the bot's `trades.jsonl` is now the label source.
- `data/theses.jsonl` history (4 months live data) — becomes git-tracked as of this pass.

## REPAIR — worth salvaging

1. **yfinance flakiness** (4 lost days: "Only 0 rows"): retry ×3 with 60s backoff around the download. ~10 lines.
2. **Watchdog shared fate** (8 silently missed days in June–July; watchdog runs on the same machine, so machine-asleep kills both): external dead-man's switch. *(Mechanism amended 2026-09-01 per Ian's remove-my-intervention request: instead of healthchecks.io — which needs a new account — the run pushes a timestamp-only heartbeat commit on every invocation and a scheduled GitHub Action alerts via Pushover if today's is missing by ~7:15 ET. Zero new accounts; design intent unchanged; healthchecks.io stays the fallback if fewer moving parts are preferred. Details in DEV_PLAN.md Phase 1b.)* Machine-off finally alerts. Local watchdog stays as second layer.
3. **Repo hygiene:** sync the drifted TODO.md (June's review-DONE edit never reached `_genai_tmp`); track `data/theses.jsonl` in git.
4. **Adjacent (bot repo, separate session):** Tactical Trading Watchdog returned LastResult=1 on 2026-08-31; `last_successful_run.json` stale since 8/28; kill switch has entries blocked ("5 losses in a row, last winner 2026-06-05"). Not MICRO scope, but it is the proof-of-need for the rebuild below.

## GUT — not salvageable

- The 5d momentum sector-rotation signal, the "rotate 5–10%" directional advice, the confidence field, the 1.5% spread gate, the 20d MA trend filter. `src/sector_rotation.py` leaves the production path (backtests remain in `research/`).
- Supersedes the CLAUDE.md locked-scope row "Week 1 signal: sector rotation only" upon Ian's go.
- Gut happens at the moment briefing v1 ships — one swap, no gap in the 6:30 push.

## REBUILD — Premarket Briefing v1

MICRO's new job: **the 6:30 ET human window into the systems that do have edge.** The bot decides for itself but nothing tells Ian what state the machine is in before the day starts (today's kill-switch discovery is the proof). Read-only, files-on-disk, no cross-project imports — all contracts already exist.

Why this lives in MICRO and not the bot: an observer inside the observed system cannot report its own death — the failing component today *is* the bot's watchdog *(corrected 2026-09-01: the bot's watchdog was in fact working — exit 1 is its alert-sent path, and it alerted same-day on 8/31. The argument survives on cleaner grounds: the bot's watchdog covers "bot missed a cycle while the machine recovered later"; only an off-machine observer covers "machine dark all day" — the 8/31 root cause was exactly the machine sleeping through its wake task — and nothing anywhere surfaces "human decision pending for N days," which is the briefing's actual job)*. MICRO is a separate process, repo, and schedule slot: an independent observer. That is also what justifies the external heartbeat (silent darkness in an observability layer is a real risk, not an inconvenience).

**Design principle (thesis review, fable_plan.md Part V): this is a health-and-pending-decisions console, not a market briefing — delta-first.** The briefing keeps a snapshot of yesterday's state and leads with what *changed* (kill switch tripped/cleared, regime flipped, position opened/closed/stopped, drawdown threshold crossed, run missed); unchanged state compresses to one calm line. Any state requiring a human decision becomes a **pending-decisions queue entry that reappears daily with its age** (*"⚠ pending 87d: kill-switch reset"*) — the anti-stall mechanism for the ecosystem's documented disease. Market context is garnish and comes last.

One Pushover, ~8 lines. **v1 core = sections 0–2** (pure file reads, no market-data dependency, ~1–2 days); **sections 3–4 ship best-effort in a 2b pass** — they need premarket quotes, the flakiest dependency (yfinance premarket is unreliable; VIX has none), so a failed quote drops the line, never the briefing:

0. **What changed + pending decisions** — the delta section and the aging queue, always first.

1. **Regime** — from `market_dashboard/data/latest.json`: composite + band, regime, shock type. Staleness check (>36h → flagged STALE).
2. **Bot health** — from `tactical_markets_trading/data/last_successful_run.json` + `account_state.json`: last-success freshness, `entry_ok` + gate reason, open position count, equity vs peak (drawdown %). Today it would have read: *"⚠ bot: entries blocked (kill_switch 5 consecutive losses), last success 8/28, equity $102.7k (−0.0% off peak)."*
3. **Positions & flip proximity** — from `strategy_state_*.json` + live quotes: TQQQ distance to its trailing stop (off `position_peak_price`), SPY distance to 200d/50d MA (router flip levels). Lines within ~2% of triggering get a ⚠ prefix; calm lines stay terse.
4. **Overnight tape** — SPY/QQQ premarket vs prior close (flag |gap| ≥ 1%), VIX level + 5d change.

Mechanics: `run_tactical.py` stays the entrypoint (scheduler untouched); `src/briefing.py` replaces `sector_rotation.py` in the call path; every briefing logged to `data/briefings.jsonl` with `signal_type: "premarket_briefing"`. ~150–200 lines. Bot JSON reads are defensive: missing key → "unknown" in the line, never a crash, plus a one-line schema-drift warning when expected keys vanish (no formal versioning — one consumer).

**Scorer ships with v1:** every ⚠ line logs its trigger values; one command computes warning **precision** (⚠ followed within 3 days by the flagged event — stop hit, MA cross, gate change) and **recall** (events that occurred with no prior-day ⚠), from bot logs + prices.

**Then a two-week read**, same trick that worked in week 1:
- (a) "I acted on a ⚠ line" → tune thresholds, consider backlog items.
- (b) "All noise" → alert-only mode (push only when something is abnormal).
- (c) "Never read it" → delivery/format wrong, not content.

**Kill criteria, defaulted up front:** after the read, default = alert-only mode unless Ian affirmatively opts into the daily push; after 8 weeks with zero acted-on ⚠ lines, default = park the project. The predecessor died of decision-stall; this one ships with sunset defaults.

## Backlog — new ideas, pick after the read

1. **Trade postmortems:** a close in bot `trades.jsonl` since yesterday → one line with realized P&L + hold time. This makes the briefing the label pipeline toward the ≥30-trade ML gate.
2. **Event awareness:** hardcoded FOMC/CPI/NFP dates (same pattern as `NYSE_HOLIDAYS`) → "FOMC decision today" line. The zero-API version of the roadmap's economic calendar.
3. **VIX term structure:** ^VIX vs ^VIX3M ratio, flag backwardation flips — genuine 24–48h information, complements MACRO's level-based VIX score.
4. **High-priority flag:** the single 6:30 push uses Pushover high priority when it contains a ⚠ line. (Intraday escalation rejected — MICRO runs once daily and cannot see intraday states.)
5. **Weekly digest (Sunday PM) — PROMOTED (thesis review):** equity vs SPY, regime-day counts, gate trips. Serves the actual top-level decision (keep or kill the ensemble), which currently has no delivery mechanism at all; compatible with alert-only mode. Ships in 2b or immediately after the read.
6. **Shadow-vs-live divergence line:** bot already writes `drift_log.jsonl` / `shadow_book.jsonl`; surface backtest-vs-live drift daily.
7. **Morning intent preview:** the bot's likely actions today, computed read-only from its own rules (MA distances, monthly rebalance calendar, stop levels) — *"today the bot will likely exit XLV (fell out of top-3)."* Better oversight (8h veto window before the 14:45 entry run) and the legitimate heir to the discretionary layer: surfaces the bot's decided rules, invents no edges. An actual veto mechanism needs a bot-side hold-file contract — bot project's call; the preview needs nothing.

**Rejected on principle:** reversion flip (no stable edge), adaptive switch (parameter luck, doubles parameter surface), ML confidence (gate unmet), HTML output (Pushover-only stands), intraday polling (this is a 6:30 product), sector dispersion line (question answered; dead options invite re-litigation), **new signals of any kind without passing methodology rules 1–7** (the documented gravitational pull), **LLM market commentary** (unfalsifiable; flaky dependency in the reliability layer), **any MICRO line that overrides or second-guesses bot decisions** (the independent-observer rationale collapses the moment the observer trades).

## Sequencing

- **Phase −1 — first, separate session in the bot repo:** bot health. Kill switch blocking entries since 2026-08-03 *(corrected 2026-09-01 from "~June 5" — that was the last winner's date, not the trip; 5th consecutive loss closed 8/3 per trades.jsonl)*, bot's own watchdog exiting 1 (8/31) *(2026-09-01: exit 1 = its alert-sent path — working as designed)*, state stale since 8/28 *(root-caused: machine slept through its 2:30 PM wake task; catch-up run correctly skipped the closed market)*, five open positions on ~$102.7k equity (**paper — verified 2026-09-01**, `paper-api.alpaca.markets` in bot `.env`). Also for this session: TQQQ trailing stop is evaluated only at the daily 14:45 run (`stop_order_id: null` on post-May trades) — no resting order protects against an intraday crash. Live risk outranks everything below.
- **Phase 0 — done (`f650684` + this revision):** plan docs + TODO.md sync. No behavior change.
- **Phase 1 (day 1):** repairs — retry + heartbeat. Ships independently; old signal keeps running so the 6:30 push never goes dark.
- **Phase 2 (days 1–2):** briefing v1 core (sections 1–2), smoke-tested against live sibling files, swapped into `run_tactical.py`. Momentum signal retires in the same commit. **Phase 2b:** sections 3–4, best-effort quotes.
- **Phase 3:** two-week read → kill-criteria defaults apply → pick from backlog.
