# REDESIGN_2026-09-02 — keep / repair / gut / rebuild (canonical execution doc)

**This is the ONE canonical execution doc.** Scope decisions here win conflicts.
Evidence and reasoning: [ASSESSMENT_2026-09-02.md](ASSESSMENT_2026-09-02.md) (frozen
session record). Implementation mechanics: [DEV_PLAN.md](DEV_PLAN.md).
**Status: ready-for-dev, HOLDING for Ian's explicit go.** Plan docs commit freely.

---

## The thesis — the binding constraint has moved

At founding (April) the constraint was *existence*. By June it was *reliability* —
and that war was won: zero weekday misses since 2026-07-08, watchdog armed and
proven, health alerts caught the one mid-run death within 24h. The remaining
14 missed days are all weekend/holiday (machine off), and every programmatic
consumer runs on weekdays.

**The binding constraint now is signal quality and noise discipline:**

1. The headline band has been **orange 107 of 118 live days and red the other 11 —
   zero green/yellow — through a +16% SPX rally.** Mechanism: `red>=1 → orange`
   headline rule × two Brief 19 thresholds (crack_spread_321, copper_gold_ratio)
   that have been pinned red 66–80% of live days. Under current thresholds the
   headline would have been orange-or-worse 55% of ALL days 2018–2026 (63% of calm
   2021). The scale is broken, not the market.
2. **0 of 29 threshold sets were ever derived from data, and none has ever been
   revised.** The one empirical recalibration in project history was silently
   reverted within hours (by a commit claiming the opposite).
3. **Alert traffic is majority plumbing** (24 staleness + 7 health + daily
   never-expiring heartbeat vs 22 market alerts in 132 days), and the 22 market
   alerts preceded a ≥3% drawdown *less* often than the base rate. The user-fatigue
   endpoint is already documented (13 ignored watchdog days in July).
4. 5.4% of the composite is a hardcoded constant (manual indicators untouched
   since April 23), and the trading-bot band gate has been dead code since 08-31
   (schedule mismatch neutralizes every read).

The rebuild is therefore **a calibration and noise-discipline rebuild**, not an
infrastructure one. No new indicators, no new alert types, no platform migration.

---

## KEEP — proven, named verbatim, do not touch

- Two-repo edit/publish workflow; 7:20 wake + 7:30 task with current
  Task Scheduler settings (battery flags, PT20M limit, WakeToRun).
- `dashboard-watchdog.yml` + repo secrets (proved itself in the July push outage).
- `_publish_to_github` backlog-push + failure-alert properties, and
  `tests/test_publish.py` pinning them.
- In-pipeline freshness health alert (it caught the 08-31 death) — behavior kept;
  its state-saving bug is repaired (R2).
- Fetch layer: retry/backoff, cache fallback, parallel fetch with
  `MAX_FETCH_WORKERS`, `StaleCacheFallback` semantics.
- `series_cadence.yaml` staleness cadences — the best-calibrated numbers in the
  repo — plus the staleness-detection machinery itself.
- `data/latest.json` sidecar and its schema — now a verified two-consumer
  contract (bot live, premarket briefing imminent). `run_timestamp` stays
  tz-naive local Central; `schema_version` stays 1.
- history.csv accumulation + log_run + prune machinery; the backtest engine,
  evaluation framework, and Brief 29's adequacy gating.
- The 281-test suite (minus the isolation repairs below).
- Locked scope decisions in CLAUDE.md (sentiment source, auction metrics,
  calendar scope, 11 buckets) — none re-opened by this plan.

## REPAIR — with sizes (S <1h, M ≈ half-day, L ≈ day+)

Cluster A — test hygiene (S+S, first commit on go):
- **R1** Patch `_maybe_refresh_backtest` and `_in_quiet_hours` in the offending
  tests; add a conftest tripwire fixture that fails any test mutating
  `output/` or `data/` (the suite corrupted production artifacts at 06:40 today,
  50 minutes before the daily run).

Cluster B — alert state machine (S each, one commit):
- **R2** Health-alert path: persist the *new* market state (band, red/orange
  lists) instead of freezing yesterday's; carry `last_health_alert_time` and
  `heartbeat_start` through `new_state` so the health debounce works and the
  31-day heartbeat actually expires after 31 days.
- **R3** `_load_state()`: tolerate corrupt/truncated state file (default state +
  logged warning) instead of crashing the run.
- **R4** `score_past_alerts`: atomic tmp+replace rewrite of alert_log.jsonl.

Cluster C — data-integrity (S each, one commit):
- **R5** Cache key includes the `years` window (dry-run currently corrupts two
  indicators' percentiles for 12h via the 2-year ^GSPC overwrite).
- **R6** Check `git add` return code in `_publish_to_github`.
- **R7** Fix `_log_feed_failure`'s call signature (feed health has never once
  been recorded).
- **R8** Make the documented dry-run actually free: gate
  `get_trigger_news_context` on `ENABLE_NEWS_TRIAGE`; gate `generate_narrative`
  under `--no-news` too (or honor the cache under `--no-cache`).
- **R9** Add `series_cadence.yaml` entries for vix_term_structure,
  crack_spread_321, natgas, copper_gold_ratio (two of these are today's pinned
  reds — a silent upstream break would freeze them red forever); delete orphan
  `oil_vol` entry.
- **R14** Reproduce and fix why Brief 17 remediation logging has produced zero
  records despite 24 eligible staleness events.
- **R16** *(added 09-02 second pass)* Step-transition breadcrumbs at INFO in
  `dashboard_run.log` (news → alerts → narrative → backtest → dashboard →
  publish) + explicit timeout on every Anthropic call. Evidence: the 08-31 death
  was a post-alert hang killed by PT20M at 07:50:02 exactly; the hang segment
  has zero log lines, so it cannot be localized even in hindsight.
- **R17** *(added 09-02 second pass)* Make the documented dry-run
  side-effect-free: `log_run` + `score_past_alerts` currently run unless
  `--ondemand`, so dry-runs append duplicate intraday history rows (26 days
  carry >1 row) and rewrite the alert log. Gate them together with alerts, or
  redocument `--ondemand` as the dry-run flag.

Cluster D — honesty of displayed numbers (S–M, one commit):
- **R13** Calibration card + `backtest_ic_summary.json`: stop quoting the
  contaminated `stress_index` ICs as "proven skill" (the target is built from
  composite inputs); headline the SPX-drawdown ICs only, with the backtest-model
  caveat (backtest has no CNN, no regime weights). Keep the adequacy gate but
  count *regimes*, not just observations, before rendering any live verdict.
- **R10** Truth in labels: "N/11 buckets" (alerts.py hardcodes /10), "29
  indicators" (dashboard hardcodes 26), history-years footer, tooltips "10
  buckets"→11, iran_trigger red=3 unreachable on a 0–2 scale.
- **R11** Fix UTF-8/cp1252 mojibake in sidecar label/unit strings.
- **R12** history.csv: log `composite_short` (currently computed daily,
  published, and unscoreable); drop the 22 vestigial columns (M — nobody
  external reads history.csv; migration is internal).

## GUT — what leaves the production path, and when

- **latest_serial.json** — orphan since May 27; delete (same commit as R12).
- **t14/t30 postmortem fields** — written, never read; stop writing (keep t7,
  which feeds the calibration card and weekly digest). Lands with Cluster D.
- **22 vestigial history.csv raw columns** — with R12.
- **Email fallback channel** — dead since birth (no `GMAIL_APP_PASSWORD` in 3
  months). Default: delete the channel (D5); Pushover + Twilio remain.
- **`_best_match_url`, `FetchOutcome` alias, EIA_API_KEY references** — delete.
- **README.md body** — replaced this session (had unresolved merge-conflict
  markers and described a 9-bucket model).
- **Manual scored indicators `iran_trigger` + `repo_stress`** — retire from the
  scored composite per D3's default; retirement lands in the SAME commit as the
  bucket reweight + bot allowlist update (no gap in delivery, see DEV_PLAN
  swap-gaps).

## REBUILD — the calibration and noise-discipline layer

- **B1 — Threshold recalibration with firing rates** (M–L). New
  `python -m src.threshold_report`: for every indicator, the historical firing
  rate of each band under current thresholds (point-in-time backtest raws,
  2018–2026 + live window), and proposed thresholds hitting target base rates
  (defaults: red ≤5% of days, orange ≤15%, per-indicator judgment overrides
  documented inline). Output = a report artifact + a proposed thresholds.yaml
  diff. **Rates get written as comments next to every threshold** (methodology
  rule 1). This is Brief 19's promised-and-never-run re-check, generalized to
  all 29. Scope additions from the 09-02 second pass: (a) flag the **never-
  firing tail too** — ten_year, nfci, treasury_auction_stress red fired 0% of
  2,263 days (a red that can't fire is as uncalibrated as one that always
  does); (b) **cnn_fear_greed is coverable** via its accumulating cache
  (16 months of real daily values; its red ≤25 fired 13% of days — same
  too-hot pattern); (c) the **composite score cutoffs 30/50/70 get the same
  treatment** — they were never derived either, and the composite's structural
  floor ≈35 makes green unreachable under any count-free rule.
- **B2 — Headline band redesign** (M, design-first, D1). Recommendation (option
  A): headline band = the *score band* of the composite, escalated at most one
  level by breadth (red indicators in ≥2 distinct buckets → +1 level), with
  one-day hysteresis. Kills the `red>=1 → orange` single-indicator veto. Option
  B (composite_short-primary headline) documented and rejected-by-default —
  see Rejected list. **Replay evidence (2018–2026, current thresholds, computed
  09-02 — % of days orange-or-worse, current rule → option A):**

  | Year | current | option A | | Episode | option A |
  |---|---|---|---|---|---|
  | 2019 | 28% | 27% | | COVID Feb–Apr 2020 | 91% o+, **74% red** (current: 47% red) |
  | 2021 | 68% | **19%** | | 2022 bear Jan–Oct | 99% o+, 64% red |
  | 2024 | 27% | 20% | | SVB Mar 2023 | 100% o+ |
  | 2025 | 90% | 41% | | live 2026 window | 100% → **17%**; today = yellow |

  Calm years drop to teens–twenties; every real stress episode stays ≥90%
  orange-or-worse and COVID gets *sharper*. Known flaw folded into the design:
  option A alone never shows green (composite floor ≈35) — hence B1's
  composite-cutoff recalibration ships with it, or yellow is accepted and
  labeled as the calm state.
- **B3 — Alert routing rebuild** (M). Two lanes: **market** (band changes, new
  reds under recalibrated thresholds — Pushover push, target <2/month in calm
  regimes) and **plumbing** (staleness, health-recovery notices, feed health —
  dashboard card + Monday digest only; push ONLY for the two critical cases:
  dashboard-stale and publish-failure). Heartbeat expires after its 31 days
  (R2) and does not return.
- **B4 — Regime-weights review (Brief 26) dissolved into an event trigger**
  (S). The calendar-date review has been overdue since 06-20 because its
  precondition (high-regime days) never occurred. Replace with: "auto-flag for
  review on the first 5 consecutive `high` regime days (a dashboard-card
  notice + one Pushover); until then `enabled: false` stands." Delete the dead
  date from weights.yaml comments (with allowlist update, see swap-gaps).

## Sequencing

- **Phase 0 (this session, docs only)** — assessment, corrections, this plan,
  DEV_PLAN. Committed today. No code.
- **Phase 1 (on go)** — Clusters A, B, C (mechanical, test-covered, no
  semantics changes). One commit per cluster.
- **Phase 2** — Cluster D + threshold_report tool (B1 evidence artifact).
  Produces the firing-rate report for Ian's review. No thresholds change yet.
- **Phase 3** — Apply recalibrated thresholds (D2 default), band redesign (B2
  after D1), alert routing (B3). Each with a year-by-year replay smoke test.
- **Phase 4** — D3/D4/D5 outcomes + B4. Coordinated commits with
  tactical_markets_trading where weights.yaml changes (allowlist).
- No Phase −1: nothing is on fire (scheduler healthy, watchdog armed, bot is
  paper, 09-02 run verified normal).

## Rejected on principle — do not re-litigate

1. **Auto-recalibration that mutates weights/thresholds silently** — already
   locked, stays locked. Recalibration produces *proposals* + a one-word apply.
2. **New indicators or buckets** until B1–B3 ship. The project added 3
   indicators while 0 thresholds got calibrated; breadth of inputs is not the
   constraint. (This includes "consumer energy burden" and any sentiment
   additions.)
3. **New alert types.** Twelve exist; the problem is routing and precision,
   not coverage.
4. **GitHub Actions publish migration, now.** Reliability is no longer
   binding; all 14 misses are weekend/holiday and all programmatic consumers
   are weekday-daytime. Revisit ONLY on the first weekday miss — the spike
   plan in TODO Phase E remains valid then.
5. **Composite_short as the headline (B2 option B).** It fixes the structural-
   level bias but silently redefines the published number two consumers read;
   a 3-year percentile window also makes 2022-style regimes look permanently
   normal. The long window stays primary; the short line stays displayed.
6. **Backtest engine rewrite** to perfectly match live scoring. The divergences
   (no CNN, no regime weights, exclude-vs-dilute on missing data) get *labeled
   honestly* on the card instead (R13). A rewrite is weeks of work to improve
   a number nobody should over-trust anyway.
7. **Paywalled news, portfolio integration** — unchanged locked/blocked status.
8. **Dashboard HTML/layout overhaul** — it serves its users; not the
   constraint.

## Guard rails — gravitational pulls this project's history proves

- **Pull: add another indicator/card/alert instead of calibrating what exists.**
  Rate-limit: any PR adding an indicator must carry its threshold firing rates.
- **Pull: silent fallback accretion.** ~35 swallow sites; a broken feed renders
  as a green dot. New rule: a fallback must either surface in the DATA QUALITY
  card or it doesn't merge.
- **Pull: flattering commit messages.** "Step 0 — restore recalibrated config"
  reverted the recalibration. Commit messages state what the diff does.
- **Pull: promising a follow-up without scheduling it.** Brief 19's "re-check
  in 2 weeks" (never ran), Brief 26's review (2.5 months overdue),
  weights.yaml's "2026-05-30" comment. Every future review gets a default
  action and a date, or it isn't written.

## Decision queue — ANSWERED by Ian 2026-09-02; execution same day

| # | Decision | Ian's pick | Status |
|---|---|---|---|
| D1 | Headline band semantics | **A: breadth-confirmed** | ✅ shipped (3B) — first live yellow in project history |
| D2 | Apply recalibrated thresholds | *pending — review `output/threshold_report.md`* | ⏳ default **apply the 5 hot-threshold proposals** on 2026-09-16; crack_spread_321 + eem_vol need a design choice (report explains) |
| D3 | Manual indicators | **automate replacements** | ✅ shipped (4A): repo_dispersion (SOFR p99−median) + gpr_daily (GPR index); allowlist updated to bcade443 |
| D4 | Bot sizing gate | **scheduled republish** | ✅ shipped: "Market Dashboard Afternoon Sidecar" task, daily 2:15 PM, first run 2026-09-03 |
| D5 | Email fallback | **delete** | ✅ shipped (2A) |

The go was given 2026-09-02; Phases 1–4 executed same day (commits a04e687 →
fb9cb01). Remaining: D2 only.

## Kill criteria for this rebuild

If the go hasn't come by **2026-10-01**, this plan compresses to a single
maintenance proposal (Clusters A–C only — pure defect repair) and the
calibration rebuild is re-pitched from current evidence; nobody executes a
month-stale plan against a moved market. If threshold recalibration ships and
after 60 days the headline band still shows >50% orange-or-worse days without
a real stress episode, the band model is declared wrong at a deeper level and
B2 gets re-opened with fresh design (not patched incrementally).
